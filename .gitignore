# Agent instruction files — kept locally, never pushed.
# AGENTS.md is regenerated by `next dev`, so it must stay ignored or it reappears.
AGENTS.md
CLAUDE.md
.claude/

# Superseded planning docs. README.md and PRD.md are the only tracked docs.
PRD-addendum-*.md
